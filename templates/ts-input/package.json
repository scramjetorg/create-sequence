{
  "name": "@scramjet/ts-input",
  "version": "0.23.0",
  "main": "index.js",
  "author": "",
  "license": "GPL-3.0",
  "description": "Scramjet Cloud Platform Sequence Typescript Template which returns input",
  "repository": {
    "type": "git",
    "url": "https://github.com/scramjetorg/platform-samples/tree/main/typescript/slack-read"
  },
  "engines": {
    "node": ">=16"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "postbuild": "cp -r package.json dist/ && (cd dist && npm i --only=production)",
    "pack": "si seq pack ./dist/",
    "clean": "rm -rf ./dist ./*.tar.gz"
  },
  "dependencies": {
  	"scramjet": "^4.36.6" },
  "devDependencies": {
    "@scramjet/types": "^0.31.2",
    "@types/node": "15.12.5",
    "scramjet": "^4.36.9",
    "typescript": "^4.9.4"
  }
}
